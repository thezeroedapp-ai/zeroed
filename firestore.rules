rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the server-side Admin SDK (Cloud Functions),
    // which bypasses these rules. No direct client access needed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
